import React from 'react';
import Nav from './Nav';

const Header = () => {
  return (
    <header>
      <div className="title">Site Title</div>
      <Nav />
    </header>
  );
};

export default Header;
